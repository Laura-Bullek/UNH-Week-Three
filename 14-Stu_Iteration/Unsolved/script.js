//WRITE YOUR CODE BELOW

// Array of five student names
let students = ["Alpha", "Beta", "Gamma", "Delta", "Epsilon"];

// Logging the total number of elements in the students array to the console
console.log (students.length);

// Using a for loop, greet each classmate and log to the console
for (var i = 0; i < students.length; i++) { 
    console.log("Great to see you " + students[i] + ".");
}